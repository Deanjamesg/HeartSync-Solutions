
<div class="d-flex justify-content-between align-items-center mb-4">
    <h1 class="h2">Upcoming Events</h1>
    <button class="btn btn-sm btn-outline-secondary"
            hx-get="@Url.Action("SignedUpEvents")"
            hx-target="#main-content-area"
            hx-push-url="@Url.Action("SignedUpEvents", "Event")">
        View My Signed-Up Events
    </button>
</div>

<div class="row g-4">
    @* @foreach (var event in Model.UpcomingEvents) { *@
    <div class="col-md-6 col-lg-4">
        <div class="card dashboard-card h-100">
            <div class="card-body d-flex flex-column">
                <h5 class="card-title">Food Drive - City Bowl</h5>
                <p class="card-text text-muted small">
                    <i class="bi bi-calendar-event"></i> Sat, Nov 15 10:00 AM<br>
                    <i class="bi bi-geo-alt"></i> Greenpoint Common
                </p>
                <p class="card-text">Join us as we distribute warm meals and supplies to those in need in the city center.</p>
                <div class="mt-auto">
                    <button class="btn btn-outline-primary btn-sm"
                            hx-get="@Url.Action("EventDetails", "Event", new { id = 1 })"
                            hx-target="#main-content-area">
                        View Details
                    </button>
                    <button class="btn btn-primary btn-sm">
                        <i class="bi bi-person-plus"></i> Sign Up
                    </button>
                </div>
            </div>
        </div>
    </div>
    @* } *@
    </div>